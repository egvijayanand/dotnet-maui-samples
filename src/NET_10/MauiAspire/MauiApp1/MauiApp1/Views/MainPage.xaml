<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="MauiApp1.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/maui/global/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MauiApp1"
    x:DataType="MainViewModel"
    ControlTemplate="{StaticResource VersionTemplate}"
    mc:Ignorable="d">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout
                Padding="20"
                Spacing="25">

                <Label
                    FontSize="32"
                    HorizontalOptions="Center"
                    SemanticProperties.HeadingLevel="Level1"
                    Style="{StaticResource MauiLabel}"
                    Text="Hello, World!" />

                <Label
                    FontSize="18"
                    HorizontalOptions="Center"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                    SemanticProperties.HeadingLevel="Level1"
                    Style="{StaticResource MauiLabel}"
                    Text="Welcome to .NET Multi-platform App UI" />

                <Button
                    Command="{Binding ForecastsCommand}"
                    HorizontalOptions="Center"
                    SemanticProperties.Hint="Updates the weather forecast"
                    Style="{StaticResource PrimaryAction}"
                    Text="Get Forecast" />

                <CollectionView
                    HorizontalOptions="Center"
                    ItemsSource="{Binding Forecasts}"
                    WidthRequest="400">
                    <!--<CollectionView.EmptyView>
                        <Label
                            HorizontalOptions="Center"
                            Text="No forecasts available."
                            VerticalOptions="Center" />
                    </CollectionView.EmptyView>-->
                    <CollectionView.Header>
                        <Grid
                            ColumnDefinitions="*,*,*"
                            ColumnSpacing="20">
                            <Label
                                FontAttributes="Bold"
                                Text="Date" />
                            <Label
                                Grid.Column="1"
                                FontAttributes="Bold"
                                Text="Temperature (F)" />
                            <Label
                                Grid.Column="2"
                                FontAttributes="Bold"
                                Text="Summary" />
                        </Grid>
                    </CollectionView.Header>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="WeatherForecast">
                            <Grid
                                Padding="0,20,0,0"
                                ColumnDefinitions="*,*,*"
                                ColumnSpacing="20">
                                <Label Text="{Binding Date, StringFormat='{0:d}'}" />
                                <Label
                                    Grid.Column="1"
                                    Text="{Binding TemperatureF}" />
                                <Label
                                    Grid.Column="2"
                                    Text="{Binding Summary}" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
